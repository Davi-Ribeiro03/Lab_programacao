#include <stdio.h>

#define pi 3.14159

int main(void) {
float ra,circ,ar,dm;

printf("Digite o valor do Raio do círculo em cm: ");
scanf("%f",&ra);

printf("\nO diâmetro é igual a %.2fcm\nO valor da circunferência é %.2fcm\nO valor da área é %.2fcm^2",dm=ra*2,circ=2*pi*ra,ar=pi*(ra*ra));
  
}